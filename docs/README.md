# Docs

Read `product/`, `decisions/`, and `runbooks/` before dated `research/`.

Source of truth is code, tests, schemas, CI, and accepted ADRs.

`archive/` is history only. Do not treat it as current product truth.
