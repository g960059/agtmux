# Changes

Use `changes/<issue-id>-slug/` only for active multi-step work.

Keep `requirements.md`, `design.md`, `plan.md`, and `tasks.md` short.

Promote durable knowledge to docs, tests, or code comments before merge, then remove the change pack from the default branch.
